name: Deploy Fees Calculator to GitHub Pages

on:
  push:
    branches: [ main ]
    paths: [ 'fees-calculator/**' ]
  pull_request:
    branches: [ main ]
    paths: [ 'fees-calculator/**' ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: fees-calculator/package-lock.json

    - name: Install dependencies
      run: |
        cd fees-calculator
        npm ci

    - name: Build
      run: |
        cd fees-calculator
        npm run build

    - name: Deploy 🚀
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./fees-calculator/build
        publish_branch: gh-pages